#lang scheme
(define funcion
       	 (lambda (lista)
                 (let funcion1 (LG LR (x 0) (y 0) (aux 8) (largo 8))
	  (cond
 	      (
                           (equal? (car(car LG) 'r')(equal?(car(car(cdr LG)) '*')
                             (begin
                               (set! x (- largo aux) x)
                               (funcion1 (car(cdr LG) 
                                
                          
	 